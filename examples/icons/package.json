{
  "name": "@examples/icons",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "maltty dev",
    "build": "maltty build",
    "routes": "maltty routes",
    "typecheck": "tsgo --noEmit"
  },
  "dependencies": {
    "maltty": "workspace:*",
    "ts-pattern": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@maltty/cli": "workspace:*",
    "tsdown": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
