{
  "name": "{{ name }}",
  "version": "0.0.0",
  "description": "{{ description }}",
  "keywords": [],
  "license": "MIT",
  "bin": {
    "{{ name }}": "./dist/index.mjs"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "scripts": {
    "build": "maltty build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "maltty": "^{{ coreVersion }}",
    "zod": "{{ zodVersion }}"
  },
  "devDependencies": {
    "@maltty/cli": "^{{ cliVersion }}",
    "tsdown": "{{ tsdownVersion }}",
    "typescript": "{{ typescriptVersion }}",
    "vitest": "{{ vitestVersion }}"
  }
}
