import { command } from '@maltty/core'
{% if includeArgs %}import { z } from 'zod'
{% endif %}
export default command({
  description: '{{ description }}',
{% if includeArgs %}  options: z.object({
    // Add your args here
  }),
{% endif %}  handler: async (ctx) => {
    // Implement your command here
  },
})
